# Core
faster-whisper>=1.0.3
sounddevice>=0.4.6
scipy>=1.11.0
numpy>=1.26.0
pyyaml>=6.0.1

# Wake word
vosk>=0.3.45

# LLM backends — cloud (all optional, install only what you use)
anthropic>=0.25.0
openai>=1.0.0
google-generativeai>=0.8.0
aider-chat>=0.50.0

# LLM backend — local (fallback, needs Ollama running)
ollama>=0.2.0